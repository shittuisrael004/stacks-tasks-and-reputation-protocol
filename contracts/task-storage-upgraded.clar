;; ---------------------------------------------------------
;; Task Storage (SECURED)
;; - Only registered users can create tasks
;; - Protocol must not be paused
;; ---------------------------------------------------------